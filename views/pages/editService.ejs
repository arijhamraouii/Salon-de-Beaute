<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modifier le Service</title>
</head>
<body>
    <h1>Modifier le service</h1>
    <form action="/services/<%= service._id %>" method="POST">
        <input type="hidden" name="_method" value="PUT"> <!-- Pour indiquer que la méthode est PUT -->

        <label for="name">Nom du service:</label>
        <input type="text" id="name" name="name" value="<%= service.name %>" required><br><br>

        <label for="price">Prix:</label>
        <input type="number" id="price" name="price" value="<%= service.price %>" required><br><br>

        <label for="description">Description:</label>
        <textarea id="description" name="description" required><%= service.description %></textarea><br><br>

        <button type="submit">Modifier</button>
    </form>
    <br>
    <a href="/services">Retour à la liste des services</a>
</body>
</html>
