<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ajouter un Employé</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="../../styles/main.css">
</head>
<body class="bg-purple-50">

    <!-- Inclusion du Header -->
    <%- include('../partials/header') %>

    <!-- Conteneur du formulaire d'ajout d'employé -->
    <div class="max-w-4xl mx-auto p-6 bg-white shadow-md rounded-lg mt-8">
        <h1 class="text-3xl font-bold text-center mb-6 text-purple-600">Ajouter un Employé</h1>

        <!-- Formulaire d'ajout -->
        <form action="/employes/ajouter" method="POST" class="space-y-6">
            
            <!-- Nom -->
            <div>
                <label for="nom" class="block text-lg font-semibold text-gray-700">Nom:</label>
                <input type="text" name="nom" required class="w-full p-3 border-2 border-purple-500 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-500">
            </div>

            <!-- Email -->
            <div>
                <label for="email" class="block text-lg font-semibold text-gray-700">Email:</label>
                <input type="email" name="email" required class="w-full p-3 border-2 border-purple-500 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-500">
            </div>

            <!-- Téléphone -->
            <div>
                <label for="telephone" class="block text-lg font-semibold text-gray-700">Téléphone:</label>
                <input type="text" name="telephone" required class="w-full p-3 border-2 border-purple-500 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-500">
            </div>

            <!-- Spécialité -->
            <div>
                <label for="specialite" class="block text-lg font-semibold text-gray-700">Spécialité:</label>
                <input type="text" name="specialite" required class="w-full p-3 border-2 border-purple-500 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-500">
            </div>

            <!-- Disponibilité -->
            <div>
                <label for="disponibilites" class="block text-lg font-semibold text-gray-700">Disponibilités:</label>
                <input type="text" name="disponibilites" class="w-full p-3 border-2 border-purple-500 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-500">
            </div>

            <!-- Bouton de soumission -->
            <div>
                <button type="submit" class="w-full p-3 mt-4 bg-purple-600 text-white font-bold rounded-lg shadow-md hover:bg-purple-700 focus:outline-none focus:ring-2 focus:ring-purple-500">
                    Ajouter
                </button>
            </div>
        </form>
    </div>

    <!-- Inclusion du Footer -->
    <%- include('../partials/footer') %>

</body>
</html>
