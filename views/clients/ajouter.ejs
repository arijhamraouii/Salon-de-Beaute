<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Ajouter un Client</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link rel="stylesheet" href="../../styles/main.css">
</head>
<body class="bg-gray-50">
    <%- include('../partials/header') %>
    <!-- Section de formulaire d'ajout de client -->
    <div class="max-w-4xl mx-auto p-6 bg-white shadow-md rounded-lg mt-8">
        <h1 class="text-3xl font-bold text-center mb-6 text-blue-600">Ajouter un Client</h1>

        <!-- Formulaire d'ajout de client -->
        <form action="/ajouter" method="POST" class="space-y-6">
            
            <!-- Nom -->
            <div>
                <label for="nom" class="block text-lg font-semibold text-gray-700">Nom:</label>
                <input type="text" id="nom" name="nom" required class="w-full p-3 border-2 border-blue-500 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>

            <!-- Prénom -->
            <div>
                <label for="prenom" class="block text-lg font-semibold text-gray-700">Prénom:</label>
                <input type="text" id="prenom" name="prenom" required class="w-full p-3 border-2 border-blue-500 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>

            <!-- Email -->
            <div>
                <label for="email" class="block text-lg font-semibold text-gray-700">Email:</label>
                <input type="email" id="email" name="email" required class="w-full p-3 border-2 border-blue-500 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>
            
            <!-- Téléphone -->
            <div>
                <label for="telephone" class="block text-lg font-semibold text-gray-700">Téléphone:</label>
                <input type="text" id="telephone" name="telephone" required class="w-full p-3 border-2 border-blue-500 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>

            <!-- Mot de passe (nécessaire pour User) -->
            <div>
                <label for="password" class="block text-lg font-semibold text-gray-700">Mot de passe:</label>
                <input type="password" id="password" name="password" required class="w-full p-3 border-2 border-blue-500 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>

            <!-- Date de naissance -->
            <div>
                <label for="dateDeNaissance" class="block text-lg font-semibold text-gray-700">Date de naissance:</label>
                <input type="date" id="dateDeNaissance" name="dateDeNaissance" required class="w-full p-3 border-2 border-blue-500 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>

            <!-- Adresse (spécifique au client) -->
            <div>
                <label for="adresse" class="block text-lg font-semibold text-gray-700">Adresse:</label>
                <input type="text" id="adresse" name="adresse" required class="w-full p-3 border-2 border-blue-500 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>

            <!-- Bouton de soumission -->
            <div>
                <button type="submit" class="w-full p-3 mt-4 bg-blue-600 text-white font-bold rounded-lg shadow-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500">
                    Ajouter le Client
                </button>
            </div>
        </form>
    </div>
    <%- include('../partials/footer') %>

</body>
</html>
